This project was created for C231 Intro to Mathematics of Cybersecurity 

The program uses a pre-determined set of keys to encode a message, then encrypt the message,
then decode the message. The reverse is done with decrypting a message

The user can put in what ever combination of letters and/or number to get an encrypted message
and decrypted message.